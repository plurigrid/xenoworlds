   class InteractiveElement:
       def __init__(self, name, position):
           self.name = name
           self.position = position

       def interact(self, agent):
           # ... code to interact with an agent ...